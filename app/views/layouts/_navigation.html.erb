<nav class="navbar navbar-expand-lg navbar-light">
  <div class="container">
    <%= link_to "WHAT ABOUT?", root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarText">
      <% if user_signed_in? %>
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <% if current_user.admin? %>
            <%= link_to "Admin Panel", admin_root_path, id:"btn#admin",class:"nav-item nav-link admin me-2" %>
          <% end %>
          <%= link_to "Posts", posts_path, class: "nav-item nav-link me-2" %>
          <%= link_to "Categories", categories_path, class: "nav-item nav-link me-2" %>
          <%= link_to 'Chatroom', rooms_path, class: "nav-item nav-link me-2"  %>
          <%= link_to "People", users_path, class: "nav-item nav-link me-2" %>
          <%= render 'search/form' %>
        </ul>   
      <% end %>
      <ul class="navbar-nav mb-2 mb-lg-0">
        <% if user_signed_in? %>
          <%= link_to current_user.username, account_path(current_user), class: "nav-item nav-link " %>
          <%= link_to destroy_user_session_path, method: :delete, data: { turbo_method: :delete }, class: "nav-item nav-link" do %>
            <i class="bi bi-box-arrow-right"></i>
          <% end %>
        <% end %>
      </ul>
    </div>
  </div>
</nav>